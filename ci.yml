Global:
  tool: build_submitter

Default:
  profile: [docSiteDeloy]

Profiles:

  - profile:
    name: docSiteDeloy
    env: cmc_standard
    command: export NODE_ENV=production && sh doc-site.sh
    release: true

