mutation projectInitCreation ($name: String, $presets: JSON) {
  projectInitCreation (name: $name, presets: $presets) {
    prompts {
      name
      type
      label
      message
      required
      default
      when
      choices {
        name
        value
        short
      }
    }
  }
}
