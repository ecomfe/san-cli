mutation projectCreation ($name: String, $presets: JSON) {
  projectCreation (name: $name, presets: $presets) {
    prompts {
      name
      type
      label
      message
      required
      default
      when
      choices {
        name
        value
        short
      }
    }
  }
}
